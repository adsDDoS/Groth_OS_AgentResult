# AI Growth OS Dashboard

Static dashboard for local setup and product demos.

Run:

```bash
npm run dashboard
```

Open:

```text
http://localhost:4173
```

The dashboard talks to the backend at `http://localhost:3000` by default. If the backend is offline, it shows a demo state and keeps company setup values in the browser.
